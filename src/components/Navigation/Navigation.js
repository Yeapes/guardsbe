import React from "react";
import Header from "./Header/Header";

const Navigation = ({ settingsData }) => {
  return (
    <>
      <Header settingsData={settingsData} />
    </>
  );
};

export default Navigation;
